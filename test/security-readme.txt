Comments for json files in the directory

- <entity>-csr.json   
    cfssl uses these files to configure <entity>'s certificate. 

- ca-config.json
    contains details of the kind of certificates the CA will issue. The signing section defines the CA's signing policy.